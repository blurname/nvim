{
  "name": "bluranmenvim",
  "private": true,
  "scripts": {
    "install-lazy": "git clone --depth 1 https://github.com/folke/lazy.nvim ~/.local/share/nvim/lazy/lazy.nvim",
    "clean-install": "rm -rf ~/.local/share/nvim/",
    "=== platform ===":"",
    "arch-linux-dep":"sudo pacaman -S python-pynvim",
    "install-lsp": "sudo npm i -g @vtsls/language-server",
    "pack":"bash pack.sh"
  }
}
